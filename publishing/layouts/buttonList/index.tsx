import React from 'react';
import DefaultLayout from '../default';
import PrimaryButton from '../../atomic/Button/PrimaryButton';

const ButtonListLayout = () => {
  return (
    <DefaultLayout>
      <PrimaryButton>label</PrimaryButton>
    </DefaultLayout>
  );
};

export default ButtonListLayout;
